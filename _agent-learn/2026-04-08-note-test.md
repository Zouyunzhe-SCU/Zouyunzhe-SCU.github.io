---
title: "笔记编写测试"
collection: agent-learn
layout: single
author_profile: true
permalink: /agent-learn/note-write
date: 2024-04-08
tags:
  - xxx
  - xxx
  - xxx
excerpt: "这是一个实例的模板,以后可以来复制这个东西来进行写作哈哈哈哈。"
read_time: true
share: true
comments: true
---


## 这样可以加粗我的标题
里面可以写一些内容啊，个人的新的和技术博客

### 这样是三级的标题
**这样可以加粗一些比较重要的东西**
- 这样是前面的小点点
- 还有一个小点点哈哈哈

### 这样可以写一些python的代码哈哈哈

```python
class VisionTransformer(nn.Module):
    def __init__(self, image_size=224, patch_size=16, num_classes=1000):
        # Patch embedding
        self.patch_embed = PatchEmbeding(image_size, patch_size)
        # Transformer encoder
        self.transformer = TransformerEncoder()
        # Classification head
        self.cls_head = nn.Linear(768, num_classes)
    
    def forward(self, x):
        x = self.patch_embed(x)  # (B, 196, 768)
        x = self.transformer(x)
        x = x[:, 0]  # CLS token
        x = self.cls_head(x)
        return x
```

> 这样可以引用一些别人的名人名言之类的哈哈哈哈

